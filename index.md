# ohl index

Hub: [[Skills]]  
Main: [[SKILL]]

Trigger summary: Destination-aware migration toolkit for OpenClaw and Hermes. Builds reviewable export packs, preserves original payload language, blocks silent overflow, and treats credentials as destination-side reissue material rather than migratable payload.

## Package map

### Main skill doc
- [[SKILL]]

- No additional bundled docs detected.

## Notes

- Boundary notes:

  - Use this when the request clearly matches the package, framework, or workflow named by the skill.

  - If a narrower sibling skill matches the exact runtime, surface, or output type, prefer that narrower skill first.

- Tuning notes:

  - Keep routing specific. This index is a navigation hub, not a generic fallback.
